import React from 'react'
import { Layout } from '../components/layouts/layout'

const Home = () => {
    return (
        <Layout>
            {/* {!isConnected && <Login />}
            {isConnected && <App />} */}

            <a>Create</a>

            <a>enter</a>

        </Layout>
    )
}


export default Home
